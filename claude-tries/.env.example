# Copie este arquivo para ".env" e preencha com seus valores reais.
# O .env de verdade NUNCA deve ser commitado (já está no .gitignore).

ANTHROPIC_API_KEY=sua-chave-aqui

# Opcionais -- só defina se quiser sobrescrever os padrões do código:
# PIPELINE_MODEL=claude-sonnet-5
# PIPELINE_REPAIR_MODEL=claude-haiku-4-5-20251001
SANDBOX_PYTHON=.venv-sandbox/bin/python
